{
  "name": "ai-engineering-playbook",
  "private": true,
  "description": "Personal AI engineering workflow standard",
  "scripts": {
    "format:docs": "prettier --write \"**/*.md\"",
    "check:docs": "prettier --check \"**/*.md\" && node scripts/check-markdown-format.js"
  },
  "devDependencies": {
    "prettier": "^3.3.0"
  }
}
